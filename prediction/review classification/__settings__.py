split = (0.2, 0.4)  # Use 30% of data for training, 30% for validation and the rest for testing.
batch_size = 64
window_size = 3  # Number of words on each side of the context window for w2v batches.
